main(){

  /// condicion ? expr1:expr2; condicion ternaria
  /// si la condicon es verdadera retor expr1 y is es falso retorna expr2
  /// 
  /// 
  

  final a=10;
  final b=5;

  final c=5;

  /// 'a es igual a 10
  final mensaje = a==10 ? 'a es igual a 10 ': 'a es diferente de 10';
  print(mensaje);

  final mensaje2 = a==c ? 'a es igual a c ': 'a es diferente de c';
  print(mensaje2);



}